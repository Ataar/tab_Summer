export interface EmployeData
{
    employeeId: string
      name: string
      position: string
      department: string
      salary: number
      hireDate: string
}

export interface FlightBooking {
  passengerName: string;
  flightNumber: string;
  destination: string;
}

